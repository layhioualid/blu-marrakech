"use client";
import { motion } from "framer-motion";
import { ArrowUpRight } from "lucide-react";
import { gallery } from "@/lib/constants";
import MotionReveal from "./MotionReveal";

export default function Gallery(){return <section id="galerie" className="section-pad bg-[#05070b]"><div className="site-container"><MotionReveal><p className="eyebrow">Inside BLU</p><h2 className="display-title">Caught in the<br/><span className="italic text-gold">moment.</span></h2></MotionReveal><div className="mt-12 grid auto-rows-[240px] gap-3 sm:grid-cols-2 lg:grid-cols-4">{gallery.map((item,i)=><motion.figure key={item.category} className={`group relative overflow-hidden border border-white/10 ${item.size}`} initial={{opacity:0,scale:.98}} whileInView={{opacity:1,scale:1}} viewport={{once:true}} transition={{delay:i*.05}}><div className="absolute inset-0 bg-[url('/images/blu-hero-cinematic.png')] bg-cover transition duration-1000 group-hover:scale-105" style={{backgroundPosition:item.position}}/><div className={`absolute inset-0 ${i%2?"bg-gradient-to-t from-ink via-electric/10 to-transparent":"bg-gradient-to-t from-ink via-transparent to-gold/5"}`}/><figcaption className="absolute inset-x-0 bottom-0 flex items-end justify-between p-5"><span><span className="block text-[8px] uppercase tracking-[.25em] text-gold">0{i+1}</span><strong className="mt-2 block font-display text-2xl font-normal">{item.category}</strong></span><ArrowUpRight className="translate-y-2 text-white/0 transition group-hover:translate-y-0 group-hover:text-white"/></figcaption></motion.figure>)}</div></div></section>}
